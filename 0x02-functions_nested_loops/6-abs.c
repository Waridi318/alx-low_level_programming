#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 *@int: argument
 * Return: absolute value of integer
 */
int _abs(int n)
{
	if (n == 0 || n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
	}
	return (-1 * n);
}
