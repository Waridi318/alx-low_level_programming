#include "main.h"

/**
 * add - adds two integers and returns the result
 * @n: integer
 * @r: integer
 * Return: result
 */
int add(int n, int r)
{
	int result;

	result = n + r;
	return (result);
}
